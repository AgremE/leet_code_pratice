from typing import List


class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        min_len = 0
        for i in range(len(nums)):
            ### Find suitable sub array from here on out:
            pass
